function Settings() {
  return (
    <div className="placeholderPage">
      <h1 className="pageTitle">Settings</h1>
      <section className="card" style={{ padding: 22 }}>
        공부 가능 시간, 시험일, 테마 설정은 다음 단계에서 추가.
      </section>
    </div>
  );
}

export default Settings;
